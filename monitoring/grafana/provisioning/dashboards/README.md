# Dashboard de reference, pas livrable final

`indusense_drift.json` est un exemple provisionne a dissequer. Le dashboard SLO
demande en M33-M34 doit etre produit par l'apprenant et ne doit pas etre une copie
de cet exemple. Aucune regle d'alerte finale ni preuve `Firing` n'est fournie ici.
