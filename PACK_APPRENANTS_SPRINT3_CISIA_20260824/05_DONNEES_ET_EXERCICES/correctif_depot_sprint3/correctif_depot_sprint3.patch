diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 8830b8d..be3ee8a 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -38,8 +38,13 @@ jobs:
       # Etape 3 : installe "uv" (gestionnaire de paquets/venv ultra-rapide).
       - uses: astral-sh/setup-uv@v3
       # Etape 4 : installe les dependances du projet + le groupe "dev".
+      #   --frozen : on installe EXACTEMENT ce qui est verrouille dans uv.lock,
+      #   sans recalculer les versions. C'est le geste enseigne dans tout le
+      #   parcours (et deja utilise par le Dockerfile) : la CI doit tester le
+      #   MEME environnement que celui qui sera livre, jamais une resolution
+      #   fraiche qui pourrait diverger.
       - name: Install
-        run: uv sync --extra dev
+        run: uv sync --frozen --extra dev
       # Etape 5 : LINT => verifie la qualite du code avec ruff.
       - name: Lint
         run: uv run ruff check .
diff --git a/docker-compose.yml b/docker-compose.yml
index 5c5bff6..78c5bfd 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -71,7 +71,7 @@ services:                 # 'services' = la liste de tous les conteneurs à lanc
       retries: 5          # Après 5 échecs consécutifs, la base est considérée comme "unhealthy".
 
   prometheus:             # --- SERVICE 3 : Prometheus, collecteur de métriques (monitoring) ---
-    image: prom/prometheus              # Image officielle Prometheus (prête à l'emploi).
+    image: prom/prometheus:v2.53.0       # Version vérifiée et mise en cache sur le poste formateur (22/08/2026).
     ports: ["9090:9090"]                # Expose l'interface web de Prometheus sur le port 9090 du PC.
     volumes: ["./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml"]
                           # On MONTE notre fichier de config local (./monitoring/prometheus.yml) à
@@ -79,7 +79,7 @@ services:                 # 'services' = la liste de tous les conteneurs à lanc
                           #   configuration (quelles cibles surveiller, à quelle fréquence...).
 
   grafana:                # --- SERVICE 4 : Grafana, tableaux de bord de visualisation ---
-    image: grafana/grafana              # Image officielle Grafana.
+    image: grafana/grafana:11.1.0        # Version vérifiée et mise en cache sur le poste formateur (22/08/2026).
     ports: ["3000:3000"]                # Interface web de Grafana accessible sur le port 3000 du PC.
     depends_on: [prometheus]            # Grafana démarre APRÈS Prometheus (sa source de données).
                           #   (ici simple ordre de démarrage, sans condition 'service_healthy').
diff --git a/src/indusense/api/main.py b/src/indusense/api/main.py
index 346c2a0..25f7d2c 100644
--- a/src/indusense/api/main.py
+++ b/src/indusense/api/main.py
@@ -84,7 +84,10 @@ from indusense.api.schemas import PredictionResponse, TabularPredictionRequest
 # Les garde-fous de sécurité définis dans security.py :
 #   - `limit_body_size` : middleware anti-payload-géant (413).
 #   - `rate_limit`      : dépendance anti-flood (429).
-from indusense.api.security import limit_body_size, rate_limit
+#   ⚠️ On branche `rate_limit_dependency` (et NON `rate_limit`) : voir sa
+#      docstring — brancher `rate_limit` directement exposerait `limit`/`window`
+#      en paramètres de requête (`?limit=100000` contournerait le garde-fou).
+from indusense.api.security import limit_body_size, rate_limit_dependency
 
 # La configuration centrale du projet (chemins, clé API, seuil de décision…).
 from indusense.config import settings
@@ -244,15 +247,17 @@ def ready(bundle: ModelBundle | None = Depends(get_model_bundle)) -> dict:
 #   - POST sur /predict-tabular (POST car le client ENVOIE des données).
 #   - `response_model=PredictionResponse` : FastAPI VALIDERA et formatera la
 #     réponse selon ce schéma (garantit le contrat de sortie + doc auto).
-#   - `dependencies=[Depends(require_api_key), Depends(rate_limit)]` : avant
-#     d'exécuter la route, FastAPI applique D'ABORD la vérification de clé API
-#     (401 si invalide) PUIS le limiteur de débit (429 si trop de requêtes).
+#   - `dependencies=[Depends(require_api_key), Depends(rate_limit_dependency)]` :
+#     avant d'exécuter la route, FastAPI applique D'ABORD la vérification de clé
+#     API (401 si invalide) PUIS le limiteur de débit (429 si trop de requêtes).
+#     (On branche `rate_limit_dependency`, pas `rate_limit` : cf. sa docstring —
+#     brancher la fonction paramétrée exposerait `?limit=` et la contournerait.)
 #     Mettre ces dépendances ici (et pas en argument) signifie « exécute-les
 #     pour leur EFFET de contrôle, on n'a pas besoin de leur valeur de retour ».
 @app.post(
     "/predict-tabular",
     response_model=PredictionResponse,
-    dependencies=[Depends(require_api_key), Depends(rate_limit)],
+    dependencies=[Depends(require_api_key), Depends(rate_limit_dependency)],
 )
 def predict_tabular(
     # `payload` : le corps JSON de la requête, automatiquement validé par Pydantic
@@ -340,7 +345,7 @@ def predict_tabular(
 # Mêmes protections que /predict-tabular : clé API (401) + anti-flood (429).
 # Cette route est `async` car lire un fichier téléversé est une opération d'I/O
 # qu'on attend avec `await` (le serveur peut faire autre chose pendant ce temps).
-@app.post("/predict-image", dependencies=[Depends(require_api_key), Depends(rate_limit)])
+@app.post("/predict-image", dependencies=[Depends(require_api_key), Depends(rate_limit_dependency)])
 async def predict_image(
     # `file: UploadFile` : FastAPI gère pour nous la réception d'un fichier envoyé
     # en « multipart/form-data ». `UploadFile` donne accès au nom, au type et au
diff --git a/src/indusense/api/security.py b/src/indusense/api/security.py
index c388064..788fc08 100644
--- a/src/indusense/api/security.py
+++ b/src/indusense/api/security.py
@@ -107,18 +107,31 @@ async def limit_body_size(request: Request, call_next):
     # (`None`) si le client ne l'a pas fournie.
     content_length = request.headers.get("content-length")
 
-    # Si l'en-tête est présent ET que la taille annoncée dépasse notre maximum :
-    #   - `content_length` est du TEXTE -> on le convertit en entier avec `int()`.
-    #   - Le test `if content_length and ...` évite de faire `int(None)` (erreur).
-    if content_length and int(content_length) > MAX_BODY_BYTES:
-        # On REFUSE immédiatement, sans appeler `call_next` : la requête n'ira
-        # jamais jusqu'à la route. On renvoie une réponse JSON avec le code 413
-        # (« Payload Too Large ») et un message d'explication en français.
-        return JSONResponse(status_code=413, content={"detail": "Payload trop volumineux"})
-
-    # Sinon (taille acceptable ou inconnue) : on laisse la requête poursuivre son
-    # chemin normalement. `await call_next(request)` exécute la suite et nous rend
-    # la réponse finale, que l'on retourne telle quelle.
+    # Si l'en-tête est présent, on l'interprète PRUDEMMENT :
+    #   - `content_length` est du TEXTE -> conversion en entier avec `int()`.
+    #   - ⚠️ un client peut envoyer une valeur ILLISIBLE (ex. "abc") : sans
+    #     protection, `int("abc")` lève une ValueError NON RATTRAPÉE -> l'API
+    #     renvoie un 500 (erreur serveur) au lieu de refuser proprement. On
+    #     encadre donc la conversion dans un try/except.
+    if content_length is not None:
+        try:
+            declared = int(content_length)
+        except ValueError:
+            # En-tête Content-Length non numérique : on ne peut pas lui faire
+            # confiance -> on rejette avec 400 (requête malformée), sans planter.
+            return JSONResponse(status_code=400, content={"detail": "Content-Length invalide"})
+        if declared > MAX_BODY_BYTES:
+            # On REFUSE immédiatement, sans appeler `call_next` : la requête n'ira
+            # jamais jusqu'à la route. Réponse JSON avec le code 413 (« Payload
+            # Too Large ») et un message d'explication en français.
+            return JSONResponse(status_code=413, content={"detail": "Payload trop volumineux"})
+
+    # ⚠️ LIMITE CONNUE (contrôle DÉCLARATIF, pas EFFECTIF) : ce garde-fou ne lit
+    # que l'EN-TÊTE annoncé. Un client qui OMET `Content-Length` (envoi en
+    # « chunked ») ou qui MENT sur sa valeur peut faire passer un corps réel plus
+    # gros. Un contrôle EFFECTIF exigerait de compter les octets reçus au niveau
+    # ASGI (au fil du flux `receive`) et/ou une limite au reverse-proxy en amont.
+    # Ici, c'est volontairement une première barrière simple — cf. module 26.
     return await call_next(request)
 
 
@@ -173,3 +186,25 @@ def rate_limit(request: Request, limit: int = 60, window: float = 60.0) -> None:
     # de la fenêtre. Puis la fonction se termine sans rien renvoyer (None) et
     # FastAPI poursuit vers la route demandée.
     q.append(now)
+
+
+# -----------------------------------------------------------------------------
+#  DÉPENDANCE à brancher sur les routes (Depends) — politique FIXE
+# -----------------------------------------------------------------------------
+def rate_limit_dependency(request: Request) -> None:
+    """Garde-fou anti-flood à brancher via `Depends(rate_limit_dependency)`.
+
+    ⚠️ POURQUOI CETTE FONCTION EXISTE (c'est une CORRECTION DE SÉCURITÉ).
+    On NE branche PAS `Depends(rate_limit)` directement. Raison : la signature de
+    `rate_limit` déclare `limit: int = 60` et `window: float = 60.0`. FastAPI
+    inspecte la signature d'une dépendance et traite tout paramètre à valeur par
+    défaut (qui n'est pas `Request`) comme un PARAMÈTRE DE REQUÊTE — il le publie
+    même dans le schéma OpenAPI. Conséquence : un client pourrait écrire
+    ``POST /predict-tabular?limit=100000`` et **désactiver** le rate limit.
+
+    Cette dépendance-ci n'a que `request` dans sa signature : rien n'est exposé
+    ni surchargeable. La politique (60 req/min/IP) est FIXE et appliquée en
+    déléguant à `rate_limit` (laissée telle quelle pour rester testable
+    directement avec des paramètres explicites).
+    """
+    rate_limit(request)
